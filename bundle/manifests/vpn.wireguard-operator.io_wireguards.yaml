apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: wireguards.vpn.wireguard-operator.io
spec:
  group: vpn.wireguard-operator.io
  names:
    kind: Wireguard
    listKind: WireguardList
    plural: wireguards
    singular: wireguard
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Wireguard is the Schema for the wireguards API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: WireguardSpec defines the desired state of Wireguard
            properties:
              address:
                description: Address for the Wireguard Service. When ServiceType is LoadBalancer, this can be used to request a fixed Service IP. If externalAddress is not set, this value is also used as the peer endpoint.
                type: string
              externalAddress:
                description: External address where peers should connect. When set, this is used for peer endpoints instead of address or Service status.
                type: string
              hostname:
                type: string
              mtu:
                type: string
              serviceAnnotations:
                additionalProperties:
                  type: string
                description: A map of key value strings for service annotations
                type: object
              serviceType:
                description: Type of Kubernetes Service to expose Wireguard.
                type: string
              peerCIDR:
                description: IPv4 CIDR range from which Wireguard peer addresses will be allocated. If omitted and ipv6Only is false, defaults to 10.8.0.0/24.
                type: string
              peerCIDRv6:
                description: IPv6 CIDR range from which Wireguard peer IPv6 addresses will be allocated. When set, IPv6 support is enabled for this Wireguard instance.
                type: string
              additionalPeerCIDRs:
                description: AdditionalPeerCIDRs is a list of additional IPv4 CIDR ranges that should be routed through the Wireguard interface. These CIDRs will be added as routes but IPs will not be allocated from them for peers.
                items:
                  type: string
                type: array
              additionalPeerCIDRv6s:
                description: AdditionalPeerCIDRv6s is a list of additional IPv6 CIDR ranges that should be routed through the Wireguard interface. These CIDRs will be added as routes but IPs will not be allocated from them for peers.
                items:
                  type: string
                type: array
              ipv6Only:
                description: When true and peerCIDRv6 is set, only IPv6 connectivity will be configured for peers (no new IPv4 addresses, routes, or NAT).
                type: boolean
              tolerations:
                items:
                  properties:
                    effect:
                      type: string
                    key:
                      type: string
                    operator:
                      type: string
                    tolerationSeconds:
                      format: int64
                      type: integer
                    value:
                      type: string
                  type: object
                type: array
              hostNetwork:
                description: HostNetwork specifies whether the agent pod should use the host's network namespace. When true, the pod will share the host's network stack.
                type: boolean
              agentHTTPPort:
                description: AgentHTTPPort specifies the HTTP port for the agent container. Defaults to 8080 if not specified.
                format: int32
                type: integer
              imagePullSecrets:
                description: ImagePullSecrets specifies the secrets to use for pulling images.
                items:
                  description: LocalObjectReference contains enough information to let you locate the referenced object inside the same namespace.
                  properties:
                    name:
                      description: Name of the referent.
                      type: string
                  type: object
                type: array
            type: object
          status:
            description: WireguardStatus defines the observed state of Wireguard
            properties:
              hostname:
                description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                  of cluster Important: Run "make" to regenerate code after modifying
                  this file'
                type: string
              message:
                type: string
              port:
                type: string
              status:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
